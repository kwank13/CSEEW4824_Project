System Call Predictor records system calls and their preceding instructions to attempt to predict
when a system call will occur

To compile:
	type 'make' - this will compile the program

To generate instruction list:
	/path/to/pin_root/pin -t obj-intel64/sys_pred.so <-o [filename]> -- <executable program>

	Use 'o' flag to change filename of output file
	Default name is 'trace.out'

To run the predictor simulation:
	python pred_syscalls.py <pin output file> <buffer length>
